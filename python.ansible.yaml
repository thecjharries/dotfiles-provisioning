---
- name: Configure python
  hosts: localhost
  connection: local

  tasks:
    - name: Upgrade pip
      ansible.builtin.pip: # noqa package-latest
        name: pip
        state: latest
      tags:
        - python
        - pip

    - name: Install important pip packages
      ansible.builtin.pip: # noqa package-latest
        state: latest
        extra_args: --user
        name:
          - asciinema
          - checkov
          - pipenv
          - pre-commit
          - romt
      tags:
        - python
        - pip
